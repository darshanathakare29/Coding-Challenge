import Navbar from "../common/Navbar";

const UserDashboard = () => {
  return (
    <div className="p-6">
      <h2 className="text-2xl font-bold mb-4">User Dashboard</h2>
      <p>Search for stores and rate them from 1 to 5.</p>
      {/* Add search bar and rating form here */}
    </div>
  );
};

export default UserDashboard;
